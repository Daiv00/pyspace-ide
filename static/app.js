let ed,project=null,file=null,role='user';const $=id=>document.getElementById(id);async function api(u,o={}){let r=await fetch(u,{...o,headers:{'Content-Type':'application/json',...(o.headers||{})}}),d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Ошибка');return d}
require.config({paths:{vs:'https://cdnjs.cloudflare.com/ajax/libs/monaco-editor/0.52.2/min/vs'}});require(['vs/editor/editor.main'],()=>{ed=monaco.editor.create($('editor'),{value:'',language:'python',theme:'vs-dark',automaticLayout:true,minimap:{enabled:true},fontSize:14,tabSize:4});boot()});
async function boot(){let m=await api('/api/me');if(m.authenticated)start(m)}async function login(){try{start(await api('/api/login',{method:'POST',body:JSON.stringify({username:$('username').value,password:$('password').value})}))}catch(e){msg(e)}}async function register(){try{await api('/api/register',{method:'POST',body:JSON.stringify({username:$('username').value,password:$('password').value}));$('msg').textContent='Аккаунт создан. Войдите.'}catch(e){msg(e)}}function msg(e){$('msg').textContent=e.message||e}function start(m){$('auth').classList.add('hidden');$('app').classList.remove('hidden');$('who').textContent=m.username;role=m.role;$('admin').style.display=role==='admin'?'inline-block':'none';loadProjects()}async function logout(){await api('/api/logout',{method:'POST'});location.reload()}
async function loadProjects(){let ps=await api('/api/projects');$('projects').innerHTML=ps.map(p=>`<div class="project ${project?.id===p.id?'active':''}" onclick='selectProject(${JSON.stringify(p)})'>▸ ${esc(p.name)} <small>${p.access}</small></div>`).join('');if(!project&&ps[0])selectProject(ps[0])}async function selectProject(p){project=p;file=null;await loadProjects();loadFiles()}async function loadFiles(){let fs=await api(`/api/projects/${project.id}/files`);$('files').innerHTML=fs.map(f=>`<div class="file ${file?.path===f.path?'active':''}" onclick='openFile(${JSON.stringify(f.path)})'>• ${esc(f.path)}</div>`).join('');if(!file&&fs[0])openFile(fs[0].path)}async function openFile(p){file=await api(`/api/projects/${project.id}/files/${encodeURIComponent(p)}`);ed.setValue(file.content);$('tab').textContent=file.path;loadFiles()}
async function newProject(){let n=prompt('Название проекта');if(!n)return;project=await api('/api/projects',{method:'POST',body:JSON.stringify({name:n})});await loadProjects();await loadFiles()}async function newFile(){if(!project)return alert('Выберите проект');let p=prompt('Путь, например src/utils.py');if(!p)return;await api(`/api/projects/${project.id}/files`,{method:'POST',body:JSON.stringify({path:p,content:''})});openFile(p)}async function save(){if(!project||!file)return;await api(`/api/projects/${project.id}/files`,{method:'POST',body:JSON.stringify({path:file.path,content:ed.getValue()})});out('✓ Сохранено: '+file.path)}async function deleteFile(){if(!file||!confirm('Удалить файл?'))return;await api(`/api/projects/${project.id}/files`,{method:'DELETE',body:JSON.stringify({path:file.path})});file=null;loadFiles()}async function renameFile(){if(!file)return;let p=prompt('Новый путь',file.path);if(!p||p===file.path)return;await api(`/api/projects/${project.id}/rename`,{method:'POST',body:JSON.stringify({old_path:file.path,new_path:p})});openFile(p)}
async function run(){if(!project||!file)return;out('▶ Выполнение...');try{let d=await api('/api/run',{method:'POST',body:JSON.stringify({project_id:project.id,path:file.path})});out((d.ok?'✓ Успешно\n\n':'✕ Ошибка\n\n')+d.output)}catch(e){out(e.message)}}async function share(){if(!project)return;let u=prompt('Логин пользователя');if(!u)return;let r=prompt('Роль: editor или viewer','editor');if(!r)return;try{await api(`/api/projects/${project.id}/share`,{method:'POST',body:JSON.stringify({username:u,role:r})});alert('Доступ выдан')}catch(e){alert(e.message)}}async function lan(){let s=await api('/api/server');modal(`<h2>LAN сервер</h2><p class="muted">Откройте с телефона в той же Wi-Fi сети:</p><div class="url">${s.url}</div><div id="qr"></div><p class="muted">Если телефон не подключается, проверьте firewall и что оба устройства в одной сети.</p>`);setTimeout(()=>new QRCode($('qr'),{text:s.url,width:190,height:190}),50)}async function admin(){if(role!=='admin')return;let u=await api('/api/admin/users');modal('<h2>Пользователи</h2>'+u.map(x=>`<div class="member"><span>${esc(x.username)} · ${x.role}</span><span><button class="ghost" onclick="setRole(${x.id},'${x.role==='admin'?'user':'admin'}')">Роль</button><button class="ghost" onclick="delUser(${x.id})">Удалить</button></span></div>`).join(''))}async function setRole(id,r){await api(`/api/admin/users/${id}/role`,{method:'POST',body:JSON.stringify({role:r})});admin()}async function delUser(id){if(confirm('Удалить пользователя?')){await api(`/api/admin/users/${id}`,{method:'DELETE'});admin()}}function out(x){$('out').textContent=x}function esc(x){return x.replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}function modal(x){$('body').innerHTML=x;$('modal').classList.remove('hidden')}function hide(){$('modal').classList.add('hidden')}window.onkeydown=e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='s'){e.preventDefault();save()}if(e.ctrlKey&&e.key==='Enter'){e.preventDefault();run()}}
